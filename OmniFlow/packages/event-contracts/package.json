{
    "name": "@omniflow/event-contracts",
    "version": "0.0.1",
    "private": true,
    "description": "NATS event bus contracts and payload schemas",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "clean": "rimraf dist"
    },
    "dependencies": {
        "zod": "^3.24.0"
    },
    "devDependencies": {
        "typescript": "^5.8.0",
        "rimraf": "^6.0.0"
    }
}